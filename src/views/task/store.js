const defaultState = {
  taskSource: {},
};

const getters = {
  taskSource: state => state.taskSource,
};

const actions = {};

const mutations = {};

export default {
  namespaced: true,
  state: defaultState,
  getters,
  actions,
  mutations,
};
